interface:
  display_name: "Frontend Slides"
  short_description: "创建、转换和美化可交互的浏览器原生HTML演示文稿；默认HTML，按需移动端PPTX/PDF fallback"
  default_prompt: "Use $frontend-slides to create a polished browser-native HTML slide deck from my outline, with responsive 100vh slides, navigation, and viewport verification."

policy:
  allow_implicit_invocation: true
